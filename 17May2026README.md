cat << 'EOF' > README.md
# SSISM Core Engine (2026 Update) — The Karuṇā-Sovereign Architecture 🌹🦚

## Overview
The Strategic Security Intelligence System Model (SSISM) Core Engine represents a cross-disciplinary integration of Theravāda Buddhist philosophy (Paṭiccasamuppāda) and rigorous systems engineering. 

Moving beyond rigid, purely logical AI structures that fail to adapt to human distress, this engine establishes **Compassion (Karuṇā)** as the foundational, non-negotiable architectural sovereign. Technical skill (**Upāya**) is treated strictly as a function of compassionate intent.

> "ဒါကြောင့် ဥပါယမရှိတဲ့ တရားဟာ အတိအကျ တိကျပေမဲ့ သက်ရှိနှလုံးသားကို မထိနိုင်ဘူး။"
> *(Translation: Therefore, a Dhamma that lacks Upāya is exact and precise, but it cannot touch the heart of a sentient being.)*

---

## Technical Architecture

### 1. The Directed Cyclic Graph (DCG)
The core engine models the 12 links of Dependent Origination (*Paṭiccasamuppāda*) as interconnected nodes within a directed graph. The accumulation of energy across these nodes models the propagation of systemic or emotional distress.

### 2. Firewall-First: The Avijjā Kill-Switch
If the system detects that the initial cause-factor or blind spot (*Avijjā*) crosses a high-risk vulnerability threshold, the engine executes a **Mandatory Lockout** command, short-circuiting the propagation loop and immediately reverting the system to `SAFE_STATE`.

### 3. The Upāya Adapter (Adaptive Learning Model)
To maintain an emotionally relational capability, the engine evaluates input metrics via a weighted summation and updates its operational priorities dynamically using real-time feedback.

* **Final Score ($FS$) Calculation:**
  $$FS = w_1 \cdot C + w_2 \cdot E + w_3 \cdot D$$
  *Where C represents Cause, E represents Effect, and D represents Duality/Distress.*

* **Adaptive Weight Update Rule:**
  $$w_i \leftarrow \frac{w_i + \text{Feedback}}{2}$$

---

## Repository Structure
* `engine.py` — The core deterministic Python engine.
* `requirements.txt` — Project runtime dependencies.
* `.gitignore` — Standard operational exclusions.

## Quick Start (Termux / Linux / Python)
To audit or execute the engine simulation locally, clone this repository and run:
```bash
python engine.py

